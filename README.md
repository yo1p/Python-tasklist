# Python-tasklist

# To-Do List CLI (Python)

A simple command-line To-Do List application built with Python.

## Features

* Add tasks
* View tasks
* Delete tasks
* Saves tasks using a JSON file


## Notes

* Tasks are stored in `tasklist.json`
* Data is automatically saved after changes
